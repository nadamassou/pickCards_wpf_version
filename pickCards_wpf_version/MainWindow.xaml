<Window x:Class="pickCards_wpf_version.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pickCards_wpf_version"
        mc:Ignorable="d"
        Title="Card Picker" Height="300" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20" >
            <Label Content="how many cards should i pick?" FontSize="20" />
            <Slider x:Name="numberOfCards" Margin="20" Maximum="15" Minimum="1" Foreground="Black" TickPlacement="BottomRight" AutoToolTipPlacement="TopLeft" IsSnapToTickEnabled="True" />
        </StackPanel>
        <Button Content="Pick some cards" HorizontalAlignment="Center" FontSize="20" VerticalAlignment="Center" Grid.Row="2" Click="Button_Click"/>
        <ListBox x:Name="listOfCards" Grid.Column="1" Grid.RowSpan="2" FontSize="20" Margin="20"/>

      

    </Grid>
</Window>
